# Import package
`import OpenPdf from 'password-protected-pdf-viewer'`

# Usage
`
<OpenPdf
  uri={URI}
  password={password}
  scale={2}
/>
`

# Props
`uri`
`password`
`scale`

# Warning
`Ensure that the cors are allowed of your PDF URI.`
